#------------------------------------------------------------------------------
# puppet apply --modulepath="$(pwd)/../.." --parser=future 00_ssh.pp
#------------------------------------------------------------------------------

class { 'ssh':
  ensure  => running,
  version => present,
}
